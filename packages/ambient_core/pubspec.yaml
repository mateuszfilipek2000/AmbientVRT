name: ambient_core
description: AmbientVRT core engine — manifest, compare, baseline, storage, and report contracts shared by all adapters.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  crypto: ^3.0.6
  image: ^4.8.0
  pixelmatch: ^1.1.2
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
