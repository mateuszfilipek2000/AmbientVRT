{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ambientvrt.dev/schemas/config.schema.json",
  "title": "AmbientVRT project config (ambient.config.yaml)",
  "description": "User-authored configuration declaring adapters, baseline storage, compare thresholds, variants, and the canonical capture environment. Authored as YAML; validated against this schema after parsing to JSON.",
  "type": "object",
  "additionalProperties": false,
  "required": ["adapters", "storage"],
  "properties": {
    "adapters": {
      "description": "One or more capture adapters to run.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/adapter" }
    },
    "storage": {
      "description": "Where baselines live. The core stays stateless: baselines only, never run history.",
      "$ref": "#/$defs/storage"
    },
    "compare": {
      "description": "Pixel comparison tuning.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "threshold": {
          "description": "Global pixelmatch threshold (0..1). Smaller is stricter. Default 0.1.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "perSnapshot": {
          "description": "Per-snapshot threshold overrides, keyed by snapshot id.",
          "type": "object",
          "additionalProperties": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          }
        }
      }
    },
    "variants": {
      "description": "Variant names the adapters should produce captures for (e.g. light, dark).",
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "canonicalEnv": {
      "description": "Reference to the canonical capture-env image (e.g. ambient/capture-env:<digest>). Captures from a different env are flagged.",
      "type": "string",
      "minLength": 1
    }
  },
  "$defs": {
    "adapter": {
      "type": "object",
      "additionalProperties": false,
      "required": ["platform"],
      "properties": {
        "platform": {
          "type": "string",
          "enum": ["flutter", "react-native"]
        },
        "projectPath": {
          "description": "Path to the project root (flutter adapter).",
          "type": "string",
          "minLength": 1
        },
        "command": {
          "description": "Optional adapter executable override. When omitted, the platform default binary name is used.",
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "storybookStaticDir": {
          "description": "Path to the built Storybook static dir (react-native adapter).",
          "type": "string",
          "minLength": 1
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "platform": { "const": "flutter" } }
          },
          "then": {
            "required": ["projectPath"]
          }
        },
        {
          "if": {
            "properties": { "platform": { "const": "react-native" } }
          },
          "then": {
            "required": ["storybookStaticDir"]
          }
        }
      ]
    },
    "storage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["backend"],
      "properties": {
        "backend": {
          "type": "string",
          "enum": ["local", "s3"]
        },
        "path": {
          "description": "Directory for the local backend's baselines.",
          "type": "string",
          "minLength": 1
        },
        "s3": {
          "description": "Connection settings for the s3 (S3-compatible) backend. Credentials are NOT stored here; they are read from the environment variables named by accessKeyEnv/secretKeyEnv.",
          "type": "object",
          "additionalProperties": false,
          "required": ["endpoint", "bucket"],
          "properties": {
            "endpoint": {
              "description": "Host name or IP of the S3 endpoint (e.g. minio.lan, s3.amazonaws.com).",
              "type": "string",
              "minLength": 1
            },
            "bucket": {
              "description": "Bucket baselines are stored in.",
              "type": "string",
              "minLength": 1
            },
            "port": {
              "description": "TCP port; omit to imply from useSSL (443/80).",
              "type": "integer",
              "minimum": 1,
              "maximum": 65535
            },
            "useSSL": {
              "description": "Connect over HTTPS. Default true.",
              "type": "boolean"
            },
            "region": {
              "description": "Optional region override (e.g. us-east-1).",
              "type": "string",
              "minLength": 1
            },
            "prefix": {
              "description": "Optional key prefix rooting every object under a bucket sub-path.",
              "type": "string",
              "minLength": 1
            },
            "pathStyle": {
              "description": "Force path-style addressing; omit to let the client decide (path style is used automatically for non-AWS endpoints like MinIO).",
              "type": "boolean"
            },
            "accessKeyEnv": {
              "description": "Name of the env var holding the access key. Default AMBIENT_S3_ACCESS_KEY.",
              "type": "string",
              "minLength": 1
            },
            "secretKeyEnv": {
              "description": "Name of the env var holding the secret key. Default AMBIENT_S3_SECRET_KEY.",
              "type": "string",
              "minLength": 1
            }
          }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "backend": { "const": "local" } }
          },
          "then": {
            "required": ["path"]
          }
        },
        {
          "if": {
            "properties": { "backend": { "const": "s3" } },
            "required": ["backend"]
          },
          "then": {
            "required": ["s3"]
          }
        }
      ]
    }
  }
}
